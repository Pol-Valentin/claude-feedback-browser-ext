## 1. Raccourcis clavier globaux

- [x] 1.1 Ajouter un listener `keydown` global dans le side panel : `Ctrl+Shift+E` → simule un clic sur le bouton Element
- [x] 1.2 Ajouter `Ctrl+Shift+S` → simule un clic sur le bouton Screenshot

## 2. Enter/Shift+Enter dans le textarea

- [x] 2.1 Vérifier et corriger le handler keydown du composer-input : Enter (sans Shift) → appeler `send()`, Shift+Enter → laisser le comportement par défaut (nouvelle ligne)
